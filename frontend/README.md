# Secure Attendance System - Frontend

This directory is intended for the web frontend application (e.g., built with Next.js or React) for Admins and Teachers.
To initialize later: `npx create-next-app@latest .`
