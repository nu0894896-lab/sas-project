# Secure Attendance System - Mobile App

This directory is intended for the mobile application (e.g., built with React Native or Flutter) for Students.
To initialize later: `npx react-native@latest init mobile` or `flutter create .`
